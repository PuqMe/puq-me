name = "puqme-api"
main = "src/index.ts"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]

[vars]
APP_ORIGIN = "https://puq.me"
CDN_BASE_URL = "https://cdn.puq.me"
# Frontend Google Client ID (must match NEXT_PUBLIC_GOOGLE_CLIENT_ID in web app)
GOOGLE_CLIENT_ID_WEB = "816336980313-053klkrfv3o8qtuum0uj3rski2hnl8q7.apps.googleusercontent.com"

# Mail (MailChannels via Cloudflare Workers)
MAIL_FROM = "noreply@puq.me"
MAIL_FROM_NAME = "PuQ.me"
MAIL_REPLY_TO = "support@puq.me"

# D1 Database
[[d1_databases]]
binding = "DB"
database_name = "puqme-db"
database_id = "b46f1d6e-f091-49dc-8ea0-926f62ff6e22"

# KV Namespaces
[[kv_namespaces]]
binding = "KV"
id = "1ec2710bcadf478fb974f49ff971720e"

# Durable Objects
[durable_objects]
bindings = [
  { name = "CHAT_ROOM", class_name = "ChatRoom" }
]

[[migrations]]
tag = "v1"
new_sqlite_classes = ["ChatRoom"]

# Secrets (set via `wrangler secret put`):
# JWT_SECRET
# JWT_REFRESH_SECRET
# S3_ENDPOINT
# S3_REGION
# S3_BUCKET
# S3_ACCESS_KEY
# S3_SECRET_KEY
# SENTRY_DSN
# SENTRY_ENVIRONMENT
# MAILCHANNELS_API_KEY  (optional — only if your Cloudflare zone requires API-key auth for MailChannels)
# GOOGLE_CLIENT_ID
